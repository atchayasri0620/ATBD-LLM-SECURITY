# ATBD Framework Configuration

# Server Settings
HOST=0.0.0.0
PORT=8000
DEBUG=True

# Ollama & LLM Configuration
OLLAMA_BASE_URL=http://127.0.0.1:11434
LLAMA_MODEL=llama3.2
LLM_TIMEOUT_SECONDS=60.0
LLM_FALLBACK_MOCK=True

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=
REDIS_AUTO_FALLBACK=True

# ATBD Feature Weights (Eq. 7 from Research Paper)
WEIGHT_TOKEN_RATE=0.25
WEIGHT_REQUEST_FREQ=0.20
WEIGHT_PROMPT_SIM=0.20
WEIGHT_SESSION_DUR=0.15
WEIGHT_OUTPUT_SIZE=0.20

# ATBD Thresholds & Reference Constants (Eq. 5, 6, 8, 9, 10)
REFERENCE_MAX_TOKEN_RATE=2000.0
REFERENCE_MAX_REQUEST_FREQ=30.0
ALLOW_THRESHOLD_MAX=30.0
DELAY_THRESHOLD_MAX=60.0
THROTTLE_THRESHOLD_MAX=80.0

MIN_DELAY_SECONDS=1.0
MAX_DELAY_SECONDS=5.0
NORMAL_MAX_OUTPUT_TOKENS=512
MIN_THROTTLED_OUTPUT_TOKENS=96
